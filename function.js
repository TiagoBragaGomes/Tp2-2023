const pessoa ={
    nome: "Th",
    idade: 12,
    peso:55
}
console.log('Object.Keys(pessoa):', Object.keys(pessoa));
console.log('Object.values(pessoa):', Object.keys(pessoa));
console.log('Object.entries(pessoa):', Object.keys(pessoa));
console.log('Object.entries(pessoa):', Object.keys(pessoa));
console.log('Object.entries(pessoa).ForEach', 
Object.keys(pessoa).forEach(e =>{
    console.log("/n object(pessoa):");
}));